# OpenAI fixtures

The normalized fixture pairs under `fixtures/normalized/openai-*` contain
synthetic old/new SDK responses and metadata. They are deterministic and do
not contain API credentials. Held-out pairs are never included in planner
packets and are used only for final repair verification.
