# Synthetic test-only fixtures

These payloads are hand-authored software tests, NOT Stripe-produced evidence. Never move them into fixtures/raw or fixtures/normalized. The control changes only API-version metadata; application behavior is identical.
