# ChangeSpec registry

`stripe/basil-subscription-period.yaml` is transcribed from the user-supplied v3 §3.1, including its human-verification marker. Phase 2 loads this known spec explicitly; no manifest/lockfile selection or independent provider re-certification is performed. Repair metadata is preserved but unused. Provider payloads remain separate and are not fabricated here.
