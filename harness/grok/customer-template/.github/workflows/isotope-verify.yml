name: Isotope verify
on: [pull_request]
permissions: { contents: read }
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: rohanbadal/isotope@main
        with: { mode: verify, reasoner: off, repair: off }
