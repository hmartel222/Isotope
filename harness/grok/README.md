# Grok provider demonstration

Run the normal repository tests for deterministic verification. To opt into an
external API check, set `XAI_API_KEY` and run `node --test tests/grok-live.test.cjs`.
The key is never written to fixtures or artifacts.
